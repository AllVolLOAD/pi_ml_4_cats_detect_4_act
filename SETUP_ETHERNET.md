# Быстрая инструкция: Прямое подключение Raspberry Pi к ноутбуку

## Что уже настроено

✅ Файл `ssh` создан - SSH включен  
✅ Статический IP настроен в `cmdline.txt`: **192.168.1.100**

## Что нужно сделать

### 1. Физическое подключение
```
Raspberry Pi (Ethernet порт) ←→ Патч-корд ←→ Ноутбук (Ethernet порт)
```

### 2. Настройка ноутбука (Windows)

1. **Win + I** → **Сеть и Интернет** → **Изменить параметры адаптера**
2. Найдите **Ethernet** → ПКМ → **Свойства**
3. **IP версии 4 (TCP/IPv4)** → **Свойства**
4. Выберите **"Использовать следующий IP-адрес"**:
   - IP-адрес: `192.168.1.1`
   - Маска: `255.255.255.0`
   - Шлюз: _(пусто)_
5. **OK** → **OK**

### 3. Запуск Pi и подключение

1. Вставьте SD-карту в Pi
2. Подключите питание
3. Подождите 2-3 минуты
4. Откройте PowerShell на ноутбуке:
   ```powershell
   ssh pi@192.168.1.100
   ```
5. Пароль по умолчанию: `raspberry`

**Готово!** Теперь вы подключены к Raspberry Pi.


